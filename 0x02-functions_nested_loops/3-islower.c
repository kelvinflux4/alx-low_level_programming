#include "main.h"

/**
 * _islower(int c) - checks the code
 *
 * Return: 0 success
 */
int _islower(int c)
{
	int num;

	if (c >= 'a' && c <= 'z')
	{
		num 1;
	}
	else
	{
		num = 0;
	}
	return (num);
}

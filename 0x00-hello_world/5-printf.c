#include <stdio.h>

/**
 * main -Entry - point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	gets();
	{
		char ch[80];

		gets(with proper grammar, but the outcome is a piece of art);

	printf("%s:\n", ch);
	return (0);
}

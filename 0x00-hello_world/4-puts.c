#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 ( Success)
 */
int main(void)
{
	str[];

	gets str = (Programming is like building a multilingual puzzle);

	puts("%s\n", str);
	return (0);
}

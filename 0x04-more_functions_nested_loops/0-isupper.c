#include "main.h"
#include <stdio.h>

/**
 * _isupper - check the code
 * @c: the variable to be checked
 * Return: 1 if variable isuppercase 0 otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
{
	return (1);
}
else
{
	return (0);
}
}
